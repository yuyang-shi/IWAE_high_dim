# @package hydra.launcher
submitit_folder: ${hydra.sweep.dir}/.submitit/%j
timeout_min: 10000
cpus_per_task: 2
tasks_per_node: 1
mem_gb: 12
name: ${hydra.job.name}
partition: standard-gpu
max_num_timeout: 0
array_parallelism: 10
exclude: "zizgpu05.cpu.stats.ox.ac.uk,"
additional_parameters: {
  "clusters": srf_gpu_01,
  "wckey": "",
  "gres": "gpu:1",
  # "nodelist": "zizgpu01.cpu.stats.ox.ac.uk",
}
# setup: ["singularity shell --nv -B $PWD iwaehighdim.sif"]
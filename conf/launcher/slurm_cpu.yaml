# @package hydra.launcher
submitit_folder: ${hydra.sweep.dir}/.submitit/%j
timeout_min: 10000
cpus_per_task: 1
tasks_per_node: 1
mem_gb: 8
name: ${hydra.job.name}
partition: high-bigbayes-cpu
max_num_timeout: 0
array_parallelism: 10
additional_parameters: {
  "clusters": swan,
  "wckey": "",
  # "nodelist": "ziz01.cpu.stats.ox.ac.uk",
}
# setup: ["singularity shell --nv -B $PWD iwaehighdim.sif"]